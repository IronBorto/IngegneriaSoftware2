import { Component } from '@angular/core';
import { HttpClient } from './HttpClient';

@Component({
  selector: 'header',
  templateUrl: './header.html'
  
})
export class Header  { name = 'Angular'; }